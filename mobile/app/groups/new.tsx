import { View, Text } from 'react-native';

export default function NewGroupScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Create Group (Phase 1)</Text>
    </View>
  );
}
